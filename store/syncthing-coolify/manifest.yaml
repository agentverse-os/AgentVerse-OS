# Импортировано из каталога coolify командой `cloudd store import coolify`; правки руками перезапишутся —
# для ручной версии поставьте `origin: manual`. Compose и метаданные — upstream, контракт (route/endpoint/env/data) — наш.
auth: own
compose: ./compose.yaml
data:
- tank/apps/syncthing-coolify
description: Syncthing synchronizes files between two or more computers in real time.
endpoint:
  port: 8384
  service: syncthing
env:
  SERVICE_URL_SYNCTHING_8384: https://${APP_DOMAIN}
gallery: []
health: null
hooks: {}
host_docker_socket: false
icon: ./logo.svg
login:
  mode: app
  note: null
  password: null
  path: null
  user: null
name: syncthing-coolify
notes:
- 'порт 22000:22000/tcp (syncthing) не публикуется: доступ только через edge/gate'
- 'порт 22000:22000/udp (syncthing) не публикуется: доступ только через edge/gate'
- 'порт 21027:21027/udp (syncthing) не публикуется: доступ только через edge/gate'
origin: coolify
provides: []
readme: null
requires: []
route:
  mode: port
  open: iframe
schema: 1
settings: []
tags:
- filestorage
- data
- synchronization
title: Syncthing
type: app
upstream:
  categories:
  - storage
  source: https://syncthing.net/
  tags:
  - filestorage
  - data
  - synchronization
  template: syncthing.yaml
website: https://syncthing.net/
