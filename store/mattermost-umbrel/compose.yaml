# mattermost-umbrel: сгенерировано импортёром (umbrel); сеть external (создаёт cloudd), порты не публикуются.
networks:
  appnet:
    external: true
    name: ${APP_NET}
services:
  app:
    depends_on:
      db:
        condition: service_healthy
    environment:
      MM_DBNAME: mattermost
      MM_PASSWORD: admin
      MM_SQLSETTINGS_DATASOURCE: postgres://admin:admin@mattermost_db_1:5432/mattermost?sslmode=disable&connect_timeout=10
      MM_USERNAME: admin
    image: alexlack/mattermost-app:v11.10.1@sha256:b4cd4d2ffb53c9eed622ea1c29482fce9340b70bd75497f7d510507ac6559ac3
    networks:
      appnet:
        aliases:
        - mattermost_app_1
    restart: on-failure
    stop_grace_period: 1m
    user: 1000:1000
    volumes:
    - ${APP_DATA_DIR}/data/app/mattermost/config:/mattermost/config:rw
    - ${APP_DATA_DIR}/data/app/mattermost/data:/mattermost/data:rw
    - ${APP_DATA_DIR}/data/app/mattermost/logs:/mattermost/logs:rw
    - ${APP_DATA_DIR}/data/app/mattermost/plugins:/mattermost/plugins:rw
    - ${APP_DATA_DIR}/data/app/mattermost/client-plugins:/mattermost/client/plugins:rw
    - /etc/localtime:/etc/localtime:ro
  db:
    environment:
      MM_DBNAME: mattermost
      MM_PASSWORD: admin
      MM_USERNAME: admin
    healthcheck:
      interval: 10s
      retries: 5
      test:
      - CMD-SHELL
      - pg_isready -U admin -d mattermost
      timeout: 5s
    image: alexlack/mattermost-db:v11.10.1@sha256:2b178fd070bda0f62c5a71b807d6ff788cae7b5bbf4935bb16ac8cc82d178f74
    networks:
      appnet:
        aliases:
        - mattermost_db_1
    restart: on-failure
    stop_grace_period: 1m
    user: 1000:1000
    volumes:
    - ${APP_DATA_DIR}/data/db:/var/lib/postgresql/data
    - /etc/localtime:/etc/localtime:ro
