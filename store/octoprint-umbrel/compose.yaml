# octoprint-umbrel: сгенерировано импортёром (umbrel); сеть external (создаёт cloudd), порты не публикуются.
networks:
  appnet:
    external: true
    name: ${APP_NET}
services:
  web:
    image: octoprint/octoprint:1.11.8@sha256:5ea0181e476597faa2800c991af4ce16903005f46b4c16666a4080ad606310bb
    networks:
      appnet:
        aliases:
        - octoprint_web_1
    privileged: true
    restart: on-failure
    volumes:
    - ${APP_DATA_DIR}/data:/octoprint
    - /dev:/dev
