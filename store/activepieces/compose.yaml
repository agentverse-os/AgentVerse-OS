# activepieces: сгенерировано импортёром (runtipi); сеть external (создаёт cloudd), порты не публикуются.
networks:
  appnet:
    external: true
    name: ${APP_NET}
services:
  activepieces:
    depends_on:
      activepieces-postgres:
        condition: service_healthy
      activepieces-redis:
        condition: service_healthy
    environment:
    - AP_ENGINE_EXECUTABLE_PATH=dist/packages/engine/main.js
    - AP_API_KEY=${AP_API_KEY}
    - AP_ENCRYPTION_KEY=${AP_ENCRYPTION_KEY}
    - AP_JWT_SECRET=${AP_JWT_SECRET}
    - AP_ENVIRONMENT=prod
    - AP_FRONTEND_URL=${APP_PROTOCOL:-http}://${APP_DOMAIN}
    - AP_WEBHOOK_TIMEOUT_SECONDS=30
    - AP_TRIGGER_DEFAULT_POLL_INTERVAL=5
    - AP_POSTGRES_DATABASE=activepieces
    - AP_POSTGRES_HOST=activepieces-postgres
    - AP_POSTGRES_PORT=5432
    - AP_POSTGRES_USERNAME=tipi
    - AP_POSTGRES_PASSWORD=${AP_POSTGRES_PASSWORD}
    - AP_EXECUTION_MODE=UNSANDBOXED
    - AP_REDIS_HOST=activepieces-redis
    - AP_REDIS_PORT=6379
    - AP_SANDBOX_RUN_TIME_SECONDS=600
    - AP_TELEMETRY_ENABLED=true
    - AP_TEMPLATES_SOURCE_URL=https://cloud.activepieces.com/api/v1/flow-templates
    image: activepieces/activepieces:0.88.4
    networks:
      appnet:
        aliases:
        - activepieces_activepieces_1
    restart: unless-stopped
  activepieces-postgres:
    environment:
    - POSTGRES_DB=activepieces
    - POSTGRES_PASSWORD=${AP_POSTGRES_PASSWORD}
    - POSTGRES_USER=tipi
    healthcheck:
      interval: 30s
      retries: 3
      test: pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB
      timeout: 30s
    image: postgres:14
    networks:
      appnet:
        aliases:
        - activepieces_activepieces-postgres_1
    restart: unless-stopped
    volumes:
    - ${APP_DATA_DIR}/data/postgres:/var/lib/postgresql/data
  activepieces-redis:
    healthcheck:
      interval: 30s
      retries: 3
      test: redis-cli ping
      timeout: 30s
    image: redis:7
    networks:
      appnet:
        aliases:
        - activepieces_activepieces-redis_1
    restart: unless-stopped
    volumes:
    - ${APP_DATA_DIR}/data/redis/:/data
